import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ImageDetail = () => {
  return <Text>Show image of beach</Text>;
};

const styles = StyleSheet.create({});

export default ImageDetail;

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ColorCounter = () => {
  return <Text>Color Counter</Text>;
};

const styles = StyleSheet.create({});

export default ColorCounter;
